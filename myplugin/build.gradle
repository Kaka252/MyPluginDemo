apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
    compile gradleApi()
    compile localGroovy()
}

// 代码仓库
repositories{
    jcenter()
}

version = PROJ_VERSION
group = PROJ_GROUP

apply from: '../bintray.gradle'